const get = require('./get')
const make = require('./make')
const parsePointer = require('./_pointer')

const getAlmost = function (pointr, json) {
  let parts = parsePointer(pointr)
}
module.exports = getAlmost
